require 'spec_helper'

describe WeatherApp do
  it 'has a version number' do
    expect(WeatherApp::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
